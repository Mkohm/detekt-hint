language: java
install: true

os: linux
dist: trusty
jdk: oraclejdk8

script:
  - ./gradlew assemble
  - gem install bundler
  - bundle install
  - bundle exec danger
  - ./gradlew test
  - ./gradlew jacocoTestReport
  - bash <(curl -s https://codecov.io/bash)
